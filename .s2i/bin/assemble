#!/bin/bash
echo "Before assembling"

# yum install perf -y

#dotnet restore "$DOTNET_STARTUP_PROJECT" --disable-parallel

/usr/libexec/s2i/assemble
rc=$?

if [ $rc -eq 0 ]; then
    echo "After successful assembling"
else
    echo "After failed assembling"
fi

exit $rc
